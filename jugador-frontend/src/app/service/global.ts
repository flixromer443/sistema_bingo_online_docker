import { environment } from '../environments/environment';

export var Global = {
    apiJugador: environment.apiJugador,
    apiGlobal: environment.apiGlobal,
    bingoHub: environment.bingoHubUrl,
};